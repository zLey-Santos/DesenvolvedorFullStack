var primeiroNumero = prompt('Digite um Número!');
var segundoNumero = prompt('Digite outro Número!');

var pNum = parseInt(primeiroNumero);
var sNum = parseInt(segundoNumero);

var res = pNum + sNum;

alert('A soma de ' + pNum + ' + ' + sNum + ' = ' + res)